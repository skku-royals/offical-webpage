export enum Role {
  User = 'User',
  Manager = 'Manager',
  Admin = 'Admin'
}

export enum AccountStatus {
  Verifying = 'Verifying',
  Enable = 'Enable',
  Disable = 'Disable'
}

export enum RosterStatus {
  Enable = 'Enable',
  Military = 'Military',
  Absence = 'Absence',
  Alumni = 'Alumni'
}

export enum RosterType {
  Athlete = 'Athlete',
  Staff = 'Staff',
  Coach = 'Coach',
  HeadCoach = 'HeadCoach'
}
